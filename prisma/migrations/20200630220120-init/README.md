# Migration `20200630220120-init`

This migration has been generated by Brandon In at 6/30/2020, 10:01:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Game" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"whosTurnId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."User" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"gameId" text  NOT NULL ,"id" text  NOT NULL ,"name" text  NOT NULL ,"symbol" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Score" (
"id" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"userId" text  NOT NULL ,"value" integer  NOT NULL DEFAULT 0,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Board" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"gameId" text  NOT NULL ,"id" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Square" (
"boardId" text  NOT NULL ,"id" text  NOT NULL ,"value" text  NOT NULL DEFAULT '',"xPosition" integer  NOT NULL ,"yPosition" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "Board_gameId" ON "public"."Board"("gameId")

ALTER TABLE "public"."Game" ADD FOREIGN KEY ("whosTurnId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."User" ADD FOREIGN KEY ("gameId")REFERENCES "public"."Game"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Score" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Board" ADD FOREIGN KEY ("gameId")REFERENCES "public"."Game"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Square" ADD FOREIGN KEY ("boardId")REFERENCES "public"."Board"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200630220120-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,73 @@
+generator client {
+    provider      = "prisma-client-js"
+    binaryTargets = ["native", "rhel-openssl-1.0.x"]
+}
+
+datasource db {
+    provider = "postgresql"
+    url      = env("DATABASE_URL")
+}
+
+model Game {
+    id                  String              @id @default(cuid())
+
+    // relations
+    whosTurn            User                @relation(name: "WhosTurn", fields: [whosTurnId], references: [id])
+    whosTurnId          String
+    users               User[]      
+    board               Board
+
+    // system
+    createdAt           DateTime            @default(now())
+    updatedAt           DateTime            @default(now())
+}
+
+model User {
+    id                  String              @id @default(cuid())
+    name                String
+    symbol              String
+
+    // relations
+    game                Game                @relation(fields: [gameId], references: [id])
+    gameId              String        
+
+    // system
+    createdAt           DateTime            @default(now())
+    updatedAt           DateTime            @default(now())
+}
+
+model Score {
+    id                  String              @id @default(cuid())
+    value               Int                 @default(0)
+
+    // relations
+    user                User                @relation(fields: [userId], references: [id])
+    userId              String        
+
+    // system
+    updatedAt           DateTime            @default(now())
+}
+
+model Board {
+    id                  String              @id @default(cuid())
+
+    // relations
+    squares             Square[]
+    game                Game                @relation(fields: [gameId], references: [id])
+    gameId              String        
+
+    // system
+    createdAt           DateTime            @default(now())
+    updatedAt           DateTime            @default(now())
+}
+
+model Square {
+    id                  String              @id @default(cuid())
+    value               String              @default("")
+    xPosition           Int
+    yPosition           Int
+
+    // relations
+    board               Board               @relation(fields: [boardId], references: [id])
+    boardId             String        
+}
```


